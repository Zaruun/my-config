- name: Install core tools
  become: true
  apt:
    name: "{{ item }}"
    state: present
  loop:
    - curl
    - htop
    - wget
    - tmux
    - build-essential
    - git
    - cmake
    - clang
    - gcc 
